# Virtual Try-On Telegram Bot

Телеграм бот для виртуальной примерки одежды с использованием генеративных ИИ моделей. Позволяет пользователям загружать свои фото и фото одежды, а затем генерировать изображения виртуальной примерки.

## ✨ Основные возможности

- **📷 Загрузка фото**: Пользователи могут загружать свои фото и фото одежды
- **🤖 AI тестирование**: Интеграция с ИИ сервисом Fashn
- **👤 Профили пользователей**: Система регистрации и управления профилями
- **💾 Хранение данных**: Автоматическое сохранение фото в Cloudinary
- **🔄 FSM состояния**: Управление состояниями пользователя через aiogram FSM
- **📊 Логирование**: Детальное логирование всех операций
- **🗄️ Персистентное хранение**: Данные пользователей сохраняются в Redis при деплоях

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
# Клонирование репозитория
git clone <repository-url>
cd virtual-dressing-room

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

```bash
# Скопируйте файл с примером переменных
cp env.example .env

# Отредактируйте .env файл, добавив ваши API ключи
nano .env
```

### 3. Настройка базы данных

```bash
# Запуск PostgreSQL и Redis через Docker
docker-compose up -d db redis

# Создание миграций
alembic revision --autogenerate -m "Initial migration"

# Применение миграций
alembic upgrade head
```

### 4. Запуск бота

```bash
# Локальный запуск
python railway_bot_new.py

# Или через Railway (продакшн)
# Автоматический деплой при push в main ветку
```

## 🏗 Структура проекта

```
app/
├── bot/                    # Телеграм бот
│   ├── handlers/           # Обработчики команд и сообщений
│   │   ├── commands.py     # Команды бота (/start, /help, /profile, etc.)
│   │   ├── photos.py       # Обработка загрузки фото
│   │   └── ai_testing.py   # Тестирование ИИ сервисов
│   ├── keyboards/          # Клавиатуры бота
│   │   └── main.py         # Основные клавиатуры
│   ├── middleware/         # Middleware для бота
│   │   ├── logging.py      # Логирование запросов
│   │   └── user_registration.py  # Автоматическая регистрация пользователей
│   ├── states.py           # FSM состояния пользователей
│   └── bot.py              # Основная логика создания и запуска бота
├── database/               # Настройки БД
│   ├── base.py             # Базовый класс для моделей
│   ├── connection.py       # Подключение к БД
│   └── async_session.py    # Асинхронные сессии
├── models/                 # SQLAlchemy модели
│   ├── user.py             # Модель пользователя
│   └── photo.py            # Модель фото
├── services/               # Бизнес-логика
│   ├── file_service.py     # Обработка и загрузка файлов
│   ├── redis_service.py    # Работа с Redis
│   └── ai_logging_service.py  # Логирование ИИ запросов
├── utils/                  # Утилиты
│   └── validators.py       # Валидация изображений
├── config.py               # Конфигурация (локальная)
└── config_prod.py          # Конфигурация (продакшн)

railway_bot_new.py          # Точка входа для Railway
migrations/                 # Alembic миграции
requirements.txt            # Python зависимости
docker-compose.yml          # Docker конфигурация
```

## 🔧 Конфигурация

### Обязательные переменные окружения:

- `BOT_TOKEN` - токен телеграм бота (получить у @BotFather)
- `DATABASE_URL` - URL базы данных PostgreSQL
- `REDIS_URL` - URL Redis сервера

### Опциональные переменные:

- `CLOUDINARY_CLOUD_NAME` - имя облака Cloudinary для хранения фото
- `CLOUDINARY_API_KEY` - API ключ Cloudinary
- `CLOUDINARY_API_SECRET` - секретный ключ Cloudinary
- `RAILWAY_ENVIRONMENT` - флаг для определения продакшн окружения

## 🤖 Команды бота

### Основные команды:
- `/start` - Начать работу с ботом
- `/help` - Список всех команд
- `/profile` - Информация о профиле пользователя
- `/clear` - Очистить все данные пользователя

### Команды загрузки фото:
- `/upload_user_photo` - Загрузить фото пользователя
- `/upload_clothing_photo` - Загрузить фото одежды

### Команды тестирования ИИ:
- `/test_fashn` - Тест Fashn API

### Дополнительные команды:
- `/subscription` - Управление подпиской (в разработке)
- `/back` - Вернуться в главное меню

## 🐳 Docker

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f app

# Остановка
docker-compose down
```

## 🗄 База данных

### Модели:

- **User** - пользователи бота с информацией о подписке и количестве генераций
- **UserPhoto** - загруженные фото пользователей (пользователь/одежда)

### Схема данных:

```sql
-- Пользователи
users:
  - id (PK)
  - telegram_id (unique)
  - username, first_name, last_name
  - subscription_type (free/premium)
  - generation_count
  - created_at, updated_at

-- Фото пользователей  
user_photos:
  - id (PK)
  - user_id (FK -> users.id)
  - photo_url
  - photo_type (user_photo/clothing)
  - cloudinary_public_id
  - created_at, updated_at
```

### Миграции:

```bash
# Создание новой миграции
alembic revision --autogenerate -m "Description"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1
```

## 🔄 FSM Состояния

Бот использует конечный автомат (FSM) для управления состояниями пользователей:

1. **`unauthorized`** - Пользователь не авторизован (до /start)
2. **`authorized`** - Пользователь авторизован (после /start)
3. **`waiting_user_photo`** - Ожидание загрузки фото пользователя
4. **`waiting_clothing_photo`** - Ожидание загрузки фото одежды
5. **`subscription_management`** - Управление подпиской
6. **`waiting_ai_response`** - Ожидание ответа от ИИ сервиса

## 🚀 Деплой

### Railway (Продакшн):
- Автоматический деплой при push в main ветку
- Переменные окружения настраиваются в Railway dashboard
- Используется `railway_bot_new.py` как точка входа

### Локальная разработка:
```bash
python railway_bot_new.py
```

## 📊 Логирование

Бот ведет детальное логирование:
- Все команды и сообщения пользователей
- Ошибки и исключения
- Запросы к ИИ сервисам
- Операции с базой данных

## 🔄 Разработка

1. Создайте ветку для новой функции
2. Внесите изменения в код
3. Создайте миграцию БД (если нужно)
4. Протестируйте изменения
5. Создайте Pull Request

## ✅ Реализованные функции

- [x] Базовая инфраструктура бота
- [x] Система регистрации пользователей
- [x] Загрузка и валидация фото
- [x] FSM управление состояниями
- [x] Интеграция с Cloudinary
- [x] Тестирование ИИ сервисов (заглушки)
- [x] Логирование операций
- [x] Деплой на Railway

## 📝 TODO

- [ ] Реальная интеграция с ИИ API
- [ ] Система платежей и подписок
- [ ] Тесты
- [ ] Админ-панель
# Force Railway redeploy
